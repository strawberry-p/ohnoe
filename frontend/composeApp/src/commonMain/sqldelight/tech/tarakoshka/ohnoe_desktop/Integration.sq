CREATE TABLE Integration (
    name TEXT NOT NULL PRIMARY KEY,
    active INTEGER NOT NULL DEFAULT 0
);

getAll:
SELECT * FROM Integration;

get:
SELECT * FROM Integration WHERE name = (?);

add:
INSERT INTO Integration(name)
VALUES (?);

toggle:
UPDATE Integration
SET active = (?)
WHERE name = (?);
